<!--index.wxml-->


<block wx:for="{{tabs}}" wx:key="index">
  <van-button custom-class="btn" round block type="primary" class="btn" data-item="{{item}}" bind:tap="foo">{{item.title}}</van-button>
</block>

